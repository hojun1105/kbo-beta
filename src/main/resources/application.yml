spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kbohub
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create, update, validate, none
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: hojun1105@gmail.com
    password: ttol ndhw ktqd ccfp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true